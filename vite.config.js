import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset URLs work on both a user site and a project site such as
  // https://username.github.io/repository-name/.
  base: './',
});
